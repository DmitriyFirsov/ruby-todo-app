<%= stylesheet_link_tag "tasks" %>
<ul class="task-list">
  <% @tasks.each do |task| %>
      <li>
        <div>Task id: <%= task.id %></div>
        <div>Task name: <%= task.name %></div>
        <div>Task description: <%= task.description %></div>
        <div>Task author: <%= task.user.name %></div>
      </li>
  <% end %>
</ul>